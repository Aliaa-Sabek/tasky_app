specVersion: "v1"
serviceId: "taskyapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskyapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
